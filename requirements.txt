# Core dependencies (pinned for reproducibility)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
pydantic>=2.0.0,<3.0.0
tqdm>=4.65.0,<5.0.0
pyarrow>=12.0.0,<16.0.0

# Optional: OpenAI support
# openai>=1.0.0,<2.0.0

# Optional: Local models (requires GPU for larger models)
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0

# Optional: DashScope/Qwen API
# dashscope>=1.20.0
